add_executable(arch4_memory_tests memory_tests.cpp)
target_compile_features(arch4_memory_tests PRIVATE cxx_std_20)
target_include_directories(arch4_memory_tests PRIVATE ${PROJECT_SOURCE_DIR}/src/shared)
add_test(NAME arch4_memory COMMAND arch4_memory_tests)

add_executable(arch4_event_tests event_tests.cpp)
target_link_libraries(arch4_event_tests PRIVATE game shared g3dlite Detour)
add_test(NAME arch4_events COMMAND arch4_event_tests)
add_executable(arch4_socket_tests socket_tests.cpp)
target_compile_features(arch4_socket_tests PRIVATE cxx_std_20)
target_link_libraries(arch4_socket_tests PRIVATE shared)
if(WIN32)
  target_link_libraries(arch4_socket_tests PRIVATE ws2_32)
endif()
add_test(NAME arch4_socket COMMAND arch4_socket_tests)
foreach(arch4_test arch4_memory_tests arch4_event_tests arch4_socket_tests)
  target_link_libraries(${arch4_test} PRIVATE cmangos-compile-option-interface)
endforeach()
if(TARGET arch4_navigation_tests)
  target_link_libraries(arch4_navigation_tests PRIVATE cmangos-compile-option-interface)
endif()
